import 'package:flutter/material.dart';

class DirecoesPage extends StatelessWidget {
  const DirecoesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Direcoes Page'),
    );
  }
}
